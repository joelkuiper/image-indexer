"""idx CLI — visual memory search.

Agentic-friendly: JSON output, clear exit codes, no interactive prompts.
Exit codes: 0=success, 1=user error, 2=system error, 3=partial failure.
"""


def main():
    """CLI entry point. Stub — implementation follows."""
    print("idx CLI not yet implemented")


if __name__ == "__main__":
    main()
